FROM ansible/ubuntu14.04-ansible

# created at 2018-06-06
MAINTAINER zivieira@163.com

RUN apt-get update && apt-get install -y nfs-common && mkdir /mnt/nfstest

CMD while true; do echo 'hello'; sleep 2; done;

